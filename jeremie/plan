=====FUNCTIONS====
Needed functions : putstr, atol, split, parse_number, mk_dict, new_entry, add_to_dict
From stdlib : malloc, free
From unistd : write
From stdio : open, read, close

atol ok
split ok?

====TYPEDEF====
typedef struct s_entry
{
	char	*key; (or int key ?)
	char	*value;
}				t_entry;

typedef struct s_dict
{
	t_entry	*first;
	t_entry *last;
	int		size;
}				t_dict;

====TO DO====
1.	Parse number : atol
					write in letters using dict : search in sorted dict ? hash function ?
